global_costmap:
  global_frame: map
  robot_base_frame: base_footprint
  origin_x: -5 # for map
  origin_y: -5 # for map
  resolution: 0.05 # for map
  always_send_full_costmap: true
  update_frequency: 1
  static_map: true
  rolling_window: false

plugins:
  {name: static_layer, type: "costmap_2d::StaticLayer"}
